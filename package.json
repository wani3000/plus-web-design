{
  "name": "pi-landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp vercel.json dist/vercel.json",
    "preview": "vite preview",
    "verify:preview": "node scripts/verify-preview.mjs",
    "verify": "npm run build && npm run verify:preview"
  },
  "devDependencies": {
    "playwright": "^1.59.0",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "lottie-web": "^5.13.0",
    "qrcode": "^1.5.4"
  }
}
